[package]
name = "bacon-cipher"
version = "0.1.0"
authors = ["Aston <astonbitecode@gmail.com>"]
edition = "2018"

[dependencies]
html5ever = { version = "0.22", optional = true }

[features]
# Enables the tags steganographer
extended-steganography = ["html5ever"]